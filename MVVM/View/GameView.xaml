<UserControl x:Class="LeJeuDeLaVie.Mvvm.View.GameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="350"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <Grid x:Name="GameGrid" VerticalAlignment="Center" HorizontalAlignment="Center">

        </Grid>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" >
            <Button Width="70" Height="20" Margin="5" Name="StartButton" Click="StartOnClick">Start</Button>
            <Button Width="70" Margin="10" Height="20" x:Name="NextButton" Click="NextOnClick">Tour Suivant</Button>
        </StackPanel>
    </Grid>
</UserControl>
